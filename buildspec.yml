version: 0.2
phases:
  build:
    commands:
      - echo Packaging Jar file ...
      - mvn clean package -DskipTests
    post_build:
        commands:
            - echo Build completed on `date`
artifacts:
    files:
        - target/skillflow-0.0.1-SNAPSHOT.jar
    discard-paths: yes